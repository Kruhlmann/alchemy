{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile Alchemy file",
      "type": "shell",
      "command": "./bin/alchemyc ${fileDirname}/${fileBasename} ${fileDirname}/${fileBasenameNoExtension}",
      "problemMatcher": []
    }
  ]
}
