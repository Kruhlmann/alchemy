{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile tasm file",
      "type": "shell",
      "command": "./bin/tasmc ${fileDirname}/${fileBasename} ${fileDirname}/${fileBasenameNoExtension}",
      "problemMatcher": []
    }
  ]
}