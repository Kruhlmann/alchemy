{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile tasm file",
      "type": "shell",
      "command": "./bin/tasmc ${file} ${fileDirname}/${fileBasenameNoExtension}",
      "problemMatcher": []
    }
  ]
}